# Evaluation results (can be committed if desired)
# *.md              # Temporarily commented to commit sample results
history/*.json      # Keep JSON files private

# Uncomment to commit evaluation reports
# !*.md
# !history/*.json
